# Sierra Leone Seed Tracker

This is a recreated React/Vite codebase based on the public preview you shared.
It is designed to closely match the visible landing page structure and styling.

## Run locally

```bash
npm install
npm run dev
```

## Build for production

```bash
npm run build
```

## Notes

- This is a reconstruction of the visible frontend, not the original private Emergent AI project files.
- You can deploy this to GitHub by pushing the folder as a new repository.
- The current build uses React + Vite with no extra UI dependencies.
